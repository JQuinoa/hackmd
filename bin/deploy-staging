#!/bin/bash

git checkout -b heroku
npm install
npm run build
npm prune --production
git add .
git commit -m"Heroku deploy"
git push PRODUCTION heroku:master -f
# git checkout develop
# git branch -D heroku
